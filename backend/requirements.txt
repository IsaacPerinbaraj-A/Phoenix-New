# SkinSight backend dependencies.
#
# IMPORTANT — install PyTorch FIRST with the CUDA build matching your GPU
# (RTX 5050-class Blackwell needs CUDA 12.8 wheels):
#
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# then:
#
#     pip install -r requirements.txt
#
# torch/torchvision are intentionally NOT pinned here so the CUDA-specific
# wheels installed above are not overwritten by CPU wheels.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
langgraph>=0.2
requests>=2.31
numpy>=1.26
opencv-python>=4.9
timm>=1.0
grad-cam>=1.5
xgboost>=2.0
pandas>=2.2
scikit-learn>=1.4

# Testing
pytest>=8.0
httpx>=0.27
